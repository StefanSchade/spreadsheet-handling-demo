= Demo Feature Matrix

This document captures the capabilities we want the demo repository to make visible.
It is not yet a user guide. It is the planning layer between the core library and a
future walkthrough-oriented demo dataset.

== Why This Lives In The Demo Repo

The demo repository is our current consumer benchmark. That makes it the right home
for:

* the feature inventory we want to demonstrate
* the reference datasets we will curate
* the walkthrough docs that explain how JSON, pipeline config, and spreadsheet output
  relate to each other

The main repository remains the source of implementation details, FTR history, and
internal architecture notes.

== Scope

The goal is not to show every internal subsystem. The goal is to show the user-facing
capabilities that matter when building or reviewing spreadsheet workflows.

== Roundtrip Stance

Our primary demo shape is:

. JSON as the source of truth
. pipeline config as the transformation contract
. Excel as the editable working format
. JSON re-export as the closing roundtrip step

This should work for most of the user-facing flows we want to demonstrate. Some
capabilities may currently be stronger on the Excel-to-JSON side than on the reverse
direction. We should track those explicitly instead of hiding them.

== Candidate Feature Set

[cols="1,1,2,2",options="header"]
|===
| Area | Capability | User-visible effect in Excel / artifacts | Likely source material

| Data model
| Multiple tables / sheets
| Workbook contains several related sheets
| consumer smoke tests, existing demo pipelines

| Data model
| Primary-key based tables
| Stable row identity and predictable references
| pipeline defaults, existing JSON fixtures

| Data model
| Foreign-key relationships
| Related tables can be validated and enriched
| FK helper FTRs, pipeline tests

| Data model
| Nested or structured source data
| JSON-side structure becomes tabular sheets
| demo extraction plugins, JSON input fixtures

| Spreadsheet UX
| Auto-filter
| Header row gets filter controls
| IR write-path and consumer smoke tests

| Spreadsheet UX
| Freeze header
| Header remains visible while scrolling
| IR write-path and overrides tests

| Spreadsheet UX
| Header styling
| Header row receives fill/bold treatment
| xlsx writer styling tests

| Spreadsheet UX
| Helper-column styling
| FK helper columns can be visually distinguished
| FK helper refactor notes, styling config

| Validation
| Dropdown / list validation
| Restricted values appear as selectable lists
| column-map and validation tests

| Validation
| Referential-integrity checks
| Broken FK relations can be detected in pipeline validation
| pipeline validation tests

| Validation
| Duplicate / missing ID checks
| Data quality issues can be surfaced before export
| pipeline validation tests

| Metadata
| Sheet-specific overrides
| Different sheets receive different options
| YAML overrides tests

| Structure
| Multi-row headers
| Workbook shows grouped header bands
| IR multiheader tests

| Structure
| Header flatten / unflatten at edges
| JSON/XML representations can use flattened field names
| helper and XML multiheader tests

| Extensibility
| Plugin transformation via dotted path
| Domain-specific enrichment can be injected into pipeline
| demo extraction plugin, consumer smoke

| I/O
| JSON to XLSX roundtrip
| Source data can be packaged into workbook output
| consumer smoke and run roundtrip tests

| I/O
| XLSX back to JSON
| Workbook edits can be extracted again
| xlsx read-path tests

| I/O
| Optional XML / CSV edge flows
| Same frame model can be emitted to alternate formats
| XML/CSV backend tests

| Rendering meta
| Named ranges
| Workbook can expose stable named regions for formulas or validations
| named range tests
|===

== Current Status Snapshot

This is only a first-pass consumer view from the demo repository.

[cols="2,1,3",options="header"]
|===
| Capability cluster | Status | Notes

| JSON -> XLSX baseline
| already demoed
| Confirmed in existing demo flows and with the new product slice pipeline.

| XLSX -> JSON baseline
| already demoed
| Confirmed for the new product slice. Flattening headers before export keeps helper columns readable after unpack.

| FK helper columns
| already demoed
| Existing helper pipeline works and the new product slice is designed to showcase it more clearly.

| Dotted-path plugin steps
| already demoed
| Existing demo extraction pipelines already use dotted plugin references.

| Sheet-specific overrides
| already demoed
| The curated product slice now uses an overrides file with visible per-sheet differences for filter, freeze, and header styling.

| Dropdown / list validation
| already demoed
| The curated product slice now adds list validations for product status, category, and branch country.

| Header styling / freeze / auto-filter
| already demoed
| Existing demo and test coverage show these, and the new product pipeline enables them explicitly.

| Multi-row headers
| already demoed
| The product multi-header slice now shows grouped Excel headers rebuilt from flat dotted JSON keys.

| XML / CSV edge flows
| partially demoed
| Present in core capabilities, but secondary from the current demo-user perspective.

| Named ranges
| already demoed
| The product workbook can now be used as an explicit named-range demo and verification target.
|===
